import styled from 'styled-components';

export const StyledHeader = styled.header`
  width: 100%;
`;

export const TopperClaim = styled.div`
  width: 100%;
  padding: 12px 0 15px;
  font-size: 16px;
  text-align: center;
  text-transform: uppercase;
  color: var(--inverted);
  background-color: var(--dark);
`;
